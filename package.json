{
  "name": "heavy-shop",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n front,back -c cyan,magenta \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "serve:lhci": "concurrently -n front,back -c cyan,magenta \"npm run dev --workspace frontend -- --host 0.0.0.0\" \"npm run start --workspace backend\"",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "analyze": "node scripts/analyze.mjs",
    "lighthouse": "node scripts/lighthouse.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
